<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "mybatis-3-config.dtd"> 
<configuration>
	
	<!-- 配置全局属性 -->
    <settings>
        <!-- 使用jdbc的getGeneratedKeys获取数据库自增主键值 -->
        <setting name="useGeneratedKeys" value="true" />

        <!-- 使用列别名替换列名 默认:true -->
        <setting name="useColumnLabel" value="true" />
	
	 <!-- 开启驼峰命名转换:Table{create_time} -> Entity{createTime} -->
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>
    
    
  <!-- <environments default="environments">
    <environment id="gpersistsql">
      <transactionManager type="JDBC" />
      <dataSource type="POOLED">
        <property name="driver" value="com.mysql.jdbc.Driver" />
        <property name="url" value="jdbc:mysql://nestletest.mysql.rds.aliyuncs.com:3306/nestle" />
        <property name="username" value="ns" />
        <property name="password" value="Nesnes456" />
      </dataSource>
    </environment>
  </environments>
  
  <mappers>
		<mapper resource="sqlxml/sqluser.xml" />
  </mappers> -->
  
</configuration>